name: Prepare and Build

on:
  workflow_call:
    inputs:
      config_file:
        description: 'Path to the JSON config file'
        required: true
        type: string
      ksu_commit:
        description: 'KSU Commit'
        required: false
        type: string
      susfs_commit:
        description: 'SUSFS Commit'
        required: false
        type: string
      feature_set:
        description: 'Feature Set'
        required: false
        type: string

jobs:
  generate-matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Read Config and Generate Matrix
        id: set-matrix
        run: |
          echo "Reading config from ${{ inputs.config_file }}"
          if [ ! -f "${{ inputs.config_file }}" ]; then
            echo "Error: Config file not found: ${{ inputs.config_file }}"
            exit 1
          fi

          MATRIX_JSON=$(cat "${{ inputs.config_file }}" | jq -c .)
          echo "Matrix: $MATRIX_JSON"
          echo "matrix=$MATRIX_JSON" >> $GITHUB_OUTPUT

  build:
    needs: generate-matrix
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.generate-matrix.outputs.matrix) }}
    uses: ./.github/workflows/build-gki.yml
    with:
      kernel_version: ${{ matrix.kernel_version }}
      ksu_commit: ${{ inputs.ksu_commit }}
      susfs_commit: ${{ inputs.susfs_commit }}
      feature_set: ${{ inputs.feature_set }}
    secrets: inherit
